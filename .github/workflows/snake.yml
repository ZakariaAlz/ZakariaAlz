name: Generate Contribution Snake (Dark)

permissions:
  contents: write

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-snake:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout full repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Generate dark snake SVG
        uses: Platane/snk@v3
        with:
          github_user_name: "ZakariaAlz"
          outputs: |
            # Append ?palette=github-dark to switch to the GitHub dark theme colors
            github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Commit & Push snake
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add github-contribution-grid-snake-dark.svg
          git commit -m "chore: update dark contribution snake" || echo "No changes to commit"
          git push
