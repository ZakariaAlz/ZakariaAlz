# .github/workflows/snake.yml
name: Generate Contribution Snake

on:
  push:
    branches: [ main ]
    paths:
      - '**.md'
      - '.github/workflows/snake.yml'
  schedule:
    - cron: '0 0 * * *'

jobs:
  build-snake:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0                   # <— fetch all commits
          persist-credentials: true        # <— so snk can commit the SVG back

      - name: Generate snake SVG
        uses: Platane/snk@v1
        with:
          github_user_name: ${{ github.repository_owner }}
          svg_out_path: github-contribution-grid-snake.svg
          # args: '--colors "66,153,225,0.8"'  # optional custom color

      - name: Commit & Push snake
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add github-contribution-grid-snake.svg
          git commit -m "chore: update contribution snake" || echo "No changes to commit"
          git push
